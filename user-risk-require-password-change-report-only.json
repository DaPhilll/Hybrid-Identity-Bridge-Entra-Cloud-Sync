{
  "displayName": "User Risk: Require Password Change (Report-Only)",
  "state": "enabledForReportingButNotEnforced",
  "conditions": {
    "users": {
      "includeUsers": ["All"],
      "excludeUsers": [
        "<EMERGENCY_ACCESS_ACCOUNT_OBJECT_ID>"
      ]
    },
    "applications": {
      "includeApplications": ["All"]
    },
    "userRiskLevels": ["high"]
  },
  "grantControls": {
    "operator": "OR",
    "builtInControls": ["passwordChange", "mfa"]
  },
  "_comment": "Requires Entra ID P2 licensing and password writeback enabled for hybrid users synced from SRV-DC01, since remediation requires writing the new password back on-premises. Deployed in report-only mode per Microsoft's guidance. Kept separate from the sign-in risk policy per Microsoft's recommendation not to combine risk conditions in a single policy. Legacy standalone Identity Protection risk policies retire October 1, 2026."
}
